#include"server.h"

int main()
{
  while(1)
  {
    /*
    printf("%s\n", data.header);
    printf("packNum:%d\n", data.packetNumber);
    printf("state:%d\n", data.state);
    printf("playerPerTeam:%d\n", data.playersPerTeam);
    */
  }

  return 0;
}
